# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.example

# Schluessel fuer die Sitzungs-Cookies, legt der Server selbst an.
# Die Mitgliederdatei daneben ist bewusst NICHT ausgenommen: sie traegt
# nur Hashes, und ohne sie kaeme nach einem Deploy niemand herein.
/daten/geheimnis

# typescript
*.tsbuildinfo

# vercel
.vercel

# preview build output
/dist
/.preview-build
*.tmp.mjs

# Zusagen zu Events und Live-Terminen — Nutzerdaten, nicht ins Repo.
/daten/zusagen.json
/daten/*.tmp
/daten/tokens.json
